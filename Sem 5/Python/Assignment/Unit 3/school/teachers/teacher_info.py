# Create a package school with two sub-packages: students → containing student_info.py (function student_details(name, age)) 
# teachers → containing teacher_info.py (function teacher_details(name, subject)) Test it by printing both student and teacher details. 

def teacher_details(name,subject):
    return f"Teacher name : {name} , subject: {subject}"